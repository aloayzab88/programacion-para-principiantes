let numberOfLines = 0;

function increseLineNumber() {
    numberOfLines++;
    console.log(`Línea #${numberOfLines}`);
}

increseLineNumber();
increseLineNumber();
increseLineNumber();
increseLineNumber();
//ts-worksheet
let names:string[] = ['Alejandro', 'Sandra', 'Freya', 'Rocket'];
let numbers:number[] = [1,2,3,4,5];

console.log(names[1]);

for (let index = 0; index < names.length; index++) {
    console.log(names[index]);
}
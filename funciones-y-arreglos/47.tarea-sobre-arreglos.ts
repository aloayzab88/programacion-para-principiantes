//ts-worksheet

let employees:string[] = ['Alejandro', 'Sandra', 'Freya'];
let salaries:number[] = [1500, 2400, 3200];

for (let index = 0; index < employees.length; index++) {
    console.log(`${employees[index]} has a salary of ${salaries[index]}`);
}